{
  "name": "assign-contact",
  "version": "0.1.0",
  "scripts": {
    "postinstall": "cd ./src/app/extensions/ && npm install && cd ../app.functions && npm install"
  },
  "author": "AntoineBr",
  "license": "MIT",
  "dependencies": {
    "google-spreadsheet": "^4.1.0"
  }
}
